version: "3.8"

services:
  mt5-auth:
    build: .
    container_name: mt5-auth
    restart: unless-stopped
    ports:
      - "6083:8080"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      - AUTH_USER=${AUTH_USER:-admin}
      - AUTH_PASS=${AUTH_PASS:-changeme}
