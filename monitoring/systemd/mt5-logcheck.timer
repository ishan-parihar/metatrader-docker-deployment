[Unit]
Description=MT5 Monitoring Weekly Log Check Timer
Requires=mt5-logcheck.service

[Timer]
# Every Sunday at 18:00 local time — adjust as needed
OnCalendar=Sun *-*-* 18:00:00
Persistent=true
RandomizedDelaySec=300

[Install]
WantedBy=timers.target
